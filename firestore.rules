rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // School / Classroom app - allow open read/write for classroom star tracking
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
